import { Styles } from '@ijstech/components';

export default Styles.style({
    $nest: {
      '.address': {
        wordBreak: 'break-all'
      },
      '.deleteRecipientBtn': {
        cursor: 'pointer'
      },
      '.copy': {
        cursor: 'pointer'
      }
    }
})